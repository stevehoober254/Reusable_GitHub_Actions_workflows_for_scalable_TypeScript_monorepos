{
  "name": "gh-actions-monorepo-template",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0",
    "eslint": "^8.50.0",
    "prettier": "^3.0.0",
    "pnpm": "^8.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "scripts": {
    "bootstrap": "pnpm install",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}