{
  "name": "apps-api",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc -p tsconfig.json && mkdir -p dist && cp -r src/public dist || true",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts",
    "test": "jest --coverage --passWithNoTests"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.5.0",
    "@types/express": "^4.17.17",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "eslint": "^8.50.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0"
  }
}